import { useNavigate } from 'react-router-dom';

const css = '.mdbnotes-root{--bg:#070b11;--panel:#0f1620;--panel2:#0b111a;--ink:#e6edf3;--muted:#94a3b2;--line:#1d2836;--accent:#56d364;--blue:#79c0ff;min-height:100vh;padding:44px 18px 90px;color:var(--ink);background:radial-gradient(1100px 560px at 85% -8%,#0e1825 0%,transparent 60%),radial-gradient(900px 480px at -8% 8%,#0c1521 0%,transparent 55%),var(--bg);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif;line-height:1.6}.mdbnotes-root *{box-sizing:border-box}.mdbnotes-root .wrap{max-width:960px;margin:auto}.mdbnotes-root .masthead,.mdbnotes-root section,.mdbnotes-root .toc-card{background:var(--panel);border:1px solid var(--line);border-radius:16px}.mdbnotes-root .masthead{padding:36px 38px;margin-bottom:14px;background:linear-gradient(180deg,#111b28,#0b111a)}.mdbnotes-root .eyebrow{margin:0 0 10px;color:var(--accent);font-size:12px;font-weight:600;letter-spacing:.3em;text-transform:uppercase}.mdbnotes-root h1{margin:0;font-size:42px;line-height:1.05;letter-spacing:-.6px}.mdbnotes-root .masthead p{max-width:68ch;margin:12px 0 0;color:var(--muted);font-size:15px}.mdbnotes-root .chips,.mdbnotes-root .toc{display:flex;flex-wrap:wrap;gap:8px}.mdbnotes-root .chips{margin-top:18px}.mdbnotes-root .chip,.mdbnotes-root code:not(pre code){border:1px solid var(--line);border-radius:7px;background:var(--panel2);padding:2px 7px;color:var(--blue);font:12px Menlo,monospace}.mdbnotes-root .chips .chip{padding:5px 11px}.mdbnotes-root .toc-card{padding:20px 24px;margin-bottom:30px}.mdbnotes-root .toc-card h3{margin:0 0 12px;color:var(--muted);font-size:13px;letter-spacing:.16em;text-transform:uppercase}.mdbnotes-root .toc a{border:1px solid var(--line);border-radius:999px;background:var(--panel2);padding:6px 12px;color:var(--ink);font-size:13px;text-decoration:none}.mdbnotes-root .toc a:hover{border-color:var(--accent);color:var(--accent)}.mdbnotes-root section{padding:30px 34px;margin-bottom:24px}.mdbnotes-root h2{display:flex;align-items:center;gap:12px;margin:0 0 8px;font-size:26px;scroll-margin-top:20px}.mdbnotes-root h2:before{width:6px;height:27px;border-radius:3px;background:var(--accent);content:""}.mdbnotes-root h3{margin:26px 0 10px;color:var(--blue);font-size:18px}.mdbnotes-root p,.mdbnotes-root li{color:#d4dde6}.mdbnotes-root .lead{margin-top:2px;color:var(--muted);font-size:14.5px}.mdbnotes-root .def,.mdbnotes-root .note{margin:12px 0;padding:12px 16px;border-radius:0 10px 10px 0;background:var(--panel2)}.mdbnotes-root .def{border-left:3px solid var(--accent)}.mdbnotes-root .def b{color:var(--accent)}.mdbnotes-root .note{border:1px dashed var(--line);border-radius:10px;color:var(--muted);font-size:14px}.mdbnotes-root pre{overflow-x:auto;margin:10px 0 4px;padding:16px 18px;border:1px solid var(--line);border-radius:10px;background:#05080d;font:13px/1.55 "SF Mono",Menlo,monospace;color:#d4dde6}.mdbnotes-root .tablewrap{overflow-x:auto;margin:14px 0;border:1px solid var(--line);border-radius:10px}.mdbnotes-root table{width:100%;border-collapse:collapse;font-size:13px}.mdbnotes-root th,.mdbnotes-root td{padding:8px 12px;border-bottom:1px solid var(--line);text-align:left;vertical-align:top}.mdbnotes-root th{background:#0a0f17;color:var(--blue);font-size:11px;letter-spacing:.05em;text-transform:uppercase}.mdbnotes-root td:first-child{color:var(--blue);font-weight:600}.mdbnotes-root tr:nth-child(even){background:#0a0f17}.mdbnotes-root .grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}@media(max-width:640px){.mdbnotes-root{padding:64px 12px}.mdbnotes-root .masthead,.mdbnotes-root section{padding:24px 20px}.mdbnotes-root h1{font-size:34px}.mdbnotes-root .grid{grid-template-columns:1fr}}';

const Code = ({ children }) => <pre>{children}</pre>;
const Table = ({ rows }) => <div className="tablewrap"><table><thead><tr><th>Command / term</th><th>Meaning / example</th></tr></thead><tbody>{rows.map(([a,b], index) => <tr key={index}><td>{a}</td><td>{b}</td></tr>)}</tbody></table></div>;

function MongoDBNotes() {
  const navigate = useNavigate();
  const toc = [['basics','Basics'],['model','Documents & Types'],['shell','Shell & Database'],['create','Create'],['read','Read / Find'],['operators','Query Operators'],['arrays','Arrays'],['update','Update'],['delete','Delete'],['aggregate','Aggregation'],['indexes','Indexes'],['relations','Relationships'],['transactions','Transactions'],['quick','Quick Revision']];
  return <div className="mdbnotes-root"><style>{css}</style>
    <button onClick={() => navigate('/sheet/DBMS')} style={{position:'fixed',top:16,left:16,zIndex:50,background:'rgba(15,22,32,.92)',color:'#e6edf3',border:'1px solid #1d2836',borderRadius:10,padding:'8px 14px',fontSize:13,fontWeight:600,cursor:'pointer'}}>← Back to DBMS</button>
    <div className="wrap">
      <header className="masthead"><p className="eyebrow">NoSQL Document Database</p><h1>MongoDB — Complete Notes</h1><p>Exam-ready MongoDB reference: concepts, shell commands, CRUD, query operators, arrays, aggregation, indexes, relationships and transactions. Every command uses one consistent <code>students</code> collection.</p><div className="chips"><span className="chip">C → insertOne / insertMany</span><span className="chip">R → find / aggregate</span><span className="chip">U → updateOne / updateMany</span><span className="chip">D → deleteOne / deleteMany</span></div></header>
      <div className="toc-card"><h3>Contents</h3><nav className="toc">{toc.map(([id,label]) => <a href={'#'+id} key={id}>{label}</a>)}</nav></div>

      <section id="basics"><h2>MongoDB Basics</h2><div className="def"><b>MongoDB</b> is a NoSQL, document-oriented database. It stores data as BSON documents rather than rows in fixed relational tables.</div><div className="grid"><div className="def"><b>Database</b><br/>Container for collections, e.g. <code>college</code>.</div><div className="def"><b>Collection</b><br/>Group of similar documents; comparable to a SQL table.</div><div className="def"><b>Document</b><br/>JSON-like record; comparable to a SQL row.</div><div className="def"><b>Field</b><br/>Key/value inside a document; comparable to a column.</div></div><Table rows={[['SQL table','MongoDB collection'],['SQL row','MongoDB document'],['SQL column','MongoDB field'],['Primary key',<> <code>_id</code> is created automatically when omitted.</>],['JOIN',<>Embedding, references, or <code>$lookup</code>.</>]]}/></section>

      <section id="model"><h2>Documents, Schema & Data Types</h2><p className="lead">MongoDB has a flexible schema: documents in one collection can have different fields. Use validation in production when a stable structure is required.</p><Code>{'{\n  _id: ObjectId("..."), name: "Asha", age: 21, active: true,\n  course: "MCA", marks: 86, skills: ["Java", "MongoDB"],\n  address: { city: "Delhi", pin: 110001 }, joinedAt: ISODate("2026-01-10")\n}'}</Code><Table rows={[['String',<code>"Asha"</code>],['Number / Decimal128','Numeric data; Decimal128 is preferred for money.'],['Boolean',<><code>true</code> or <code>false</code></>],['Array',<code>["Java", "MongoDB"]</code>],['Embedded document',<code>{'{ city: "Delhi" }'}</code>],['ObjectId / Date / Null','Unique id, timestamp, and missing value.']]}/></section>

      <section id="shell"><h2>mongosh & Database Commands</h2><Code>{'mongosh\n\nshow dbs                 // list databases\nuse college              // switch/create database\ndb                       // current database\ndb.dropDatabase()        // delete current database\n\nshow collections\ndb.createCollection("students")\ndb.students.drop()       // delete collection and its indexes'}</Code><div className="note"><b>Remember:</b> <code>use college</code> does not persist a database until you add data to it.</div></section>

      <section id="create"><h2>Create — Insert Documents</h2><Code>{'// Insert one document\ndb.students.insertOne({\n  name: "Asha", age: 21, course: "MCA", marks: 86,\n  skills: ["Java", "MongoDB"], address: { city: "Delhi" }, active: true\n})\n\n// Insert multiple documents\ndb.students.insertMany([\n  { name: "Ravi", age: 22, course: "BCA", marks: 72, skills: ["Python"], active: true },\n  { name: "Neha", age: 20, course: "MCA", marks: 91, skills: ["Java", "React"], active: false }\n])'}</Code><p>MongoDB returns <code>acknowledged: true</code> and generated <code>insertedId</code>(s).</p></section>

      <section id="read"><h2>Read — find, Projection, Sort & Limit</h2><Code>{'db.students.find()                              // all documents\ndb.students.findOne({ name: "Asha" })           // first matching document\ndb.students.find({ marks: { $gte: 80 } })        // filter\n\n// Projection: 1 includes, 0 excludes; _id is included by default\ndb.students.find({ course: "MCA" }, { name: 1, marks: 1, _id: 0 })\n\ndb.students.find().sort({ marks: -1 }).skip(0).limit(5)\ndb.students.countDocuments({ active: true })'}</Code><div className="note"><b>Projection rule:</b> Do not mix inclusion and exclusion, except <code>_id: 0</code> may accompany included fields.</div></section>

      <section id="operators"><h2>Query Operators</h2><Table rows={[[<><code>$eq</code>, <code>$ne</code></>,'Equal / not equal'],[<><code>$gt</code>, <code>$gte</code>, <code>$lt</code>, <code>$lte</code></>,'Comparisons'],[<><code>$in</code>, <code>$nin</code></>,'Value is/is not in a list'],[<><code>$and</code>, <code>$or</code>, <code>$nor</code>, <code>$not</code></>,'Logical conditions'],[<code>$exists</code>,'Whether a field exists'],[<code>$regex</code>,'Pattern matching']]}/><Code>{'// AND is implicit for separate fields\ndb.students.find({ course: "MCA", marks: { $gte: 80 } })\ndb.students.find({ $or: [{ course: "MCA" }, { marks: { $gt: 90 } }] })\ndb.students.find({ marks: { $gte: 70, $lte: 90 } })\ndb.students.find({ course: { $in: ["MCA", "BCA"] } })\ndb.students.find({ name: { $regex: "^a", $options: "i" } })\ndb.students.find({ phone: { $exists: true } })'}</Code></section>

      <section id="arrays"><h2>Arrays & Embedded Documents</h2><Code>{'db.students.find({ skills: "Java" })\ndb.students.find({ skills: { $all: ["Java", "MongoDB"] } })\ndb.students.find({ skills: { $size: 2 } })\ndb.students.find({ skills: { $in: ["Java", "Python"] } })\n\ndb.students.find({ "address.city": "Delhi" }) // dot notation\n\ndb.scores.find({\n  results: { $elemMatch: { score: { $gte: 80 }, subject: "DBMS" } }\n})'}</Code><div className="note"><b><code>$elemMatch</code>:</b> one array element must satisfy every condition.</div></section>

      <section id="update"><h2>Update Documents</h2><Code>{'db.students.updateOne(\n  { name: "Asha" },\n  { $set: { marks: 90, "address.city": "Noida" } }\n)\ndb.students.updateMany({ course: "MCA" }, { $inc: { marks: 2 } })\n\ndb.students.updateOne({ name: "Asha" }, { $push: { skills: "Node.js" } })\ndb.students.updateOne({ name: "Asha" }, { $addToSet: { skills: "MongoDB" } })\ndb.students.updateOne({ name: "Asha" }, { $pull: { skills: "Java" } })\n\ndb.students.updateOne({ email: "a@x.com" }, { $set: { name: "Asha" } }, { upsert: true })'}</Code><Table rows={[[<code>$set / $unset</code>,'Add/change a field / remove a field'],[<code>$inc / $mul</code>,'Add to / multiply a number'],[<code>$rename</code>,'Rename a field'],[<code>$push / $pop</code>,'Add to / remove an array end item'],[<code>$addToSet</code>,'Add only if not already present'],[<code>$pull</code>,'Remove all matching array values']]}/></section>

      <section id="delete"><h2>Delete Documents</h2><Code>{'db.students.deleteOne({ name: "Ravi" })\ndb.students.deleteMany({ active: false })\ndb.students.deleteMany({})  // all documents; collection stays\ndb.students.drop()          // collection and its indexes\ndb.dropDatabase()'}</Code><div className="note"><b>Exam difference:</b> <code>deleteMany({})</code> clears documents only; <code>drop()</code> removes the collection and its indexes.</div></section>

      <section id="aggregate"><h2>Aggregation Pipeline</h2><div className="def"><b>Aggregation</b> processes documents one stage at a time. The output of a stage becomes input for the next.</div><Code>{'db.students.aggregate([\n  { $match: { marks: { $gte: 60 } } },\n  { $group: {\n      _id: "$course",\n      averageMarks: { $avg: "$marks" },\n      total: { $sum: 1 }\n  }},\n  { $sort: { averageMarks: -1 } },\n  { $project: { _id: 0, course: "$_id", averageMarks: 1, total: 1 } }\n])\n\n// One output document per skills array item\ndb.students.aggregate([{ $unwind: "$skills" }, { $group: { _id: "$skills", students: { $sum: 1 } } }])'}</Code><Table rows={[[<code>$match</code>,'Filter early (like WHERE)'],[<code>$project</code>,'Select, reshape or rename fields'],[<code>$group</code>,'Group by _id (like GROUP BY)'],[<code>$sort / $skip / $limit</code>,'Order and paginate'],[<code>$unwind</code>,'One document per array element'],[<code>$sum / $avg / $min / $max / $push</code>,'Common group accumulators'],[<code>$lookup</code>,'Join another collection']]}/></section>

      <section id="indexes"><h2>Indexes</h2><p className="lead">Indexes make reads faster but use disk/RAM and add write overhead.</p><Code>{'db.students.createIndex({ email: 1 }, { unique: true })\ndb.students.createIndex({ course: 1, marks: -1 }) // compound index\ndb.students.getIndexes()\ndb.students.dropIndex("email_1")\ndb.students.find({ email: "a@x.com" }).explain("executionStats")'}</Code><div className="note"><b>Compound index:</b> order matters. <code>{'{ course: 1, marks: -1 }'}</code> supports queries beginning with <code>course</code>.</div></section>

      <section id="relations"><h2>Relationships & $lookup</h2><div className="grid"><div className="def"><b>Embedding</b><br/>Store related data inside a document. Use when it is read together and bounded.</div><div className="def"><b>Referencing</b><br/>Store ids from another collection. Use for reusable, large, or many-to-many data.</div></div><Code>{'// orders: { studentId: ObjectId("..."), amount: 499 }\ndb.orders.aggregate([\n  { $lookup: {\n      from: "students", localField: "studentId",\n      foreignField: "_id", as: "student"\n  }},\n  { $unwind: "$student" },\n  { $project: { amount: 1, studentName: "$student.name" } }\n])'}</Code></section>

      <section id="transactions"><h2>Transactions, Backup & Validation</h2><p>MongoDB supports ACID transactions for multiple operations. Use them when related writes must all succeed or all fail; they require a replica set or sharded cluster.</p><Code>{'const session = db.getMongo().startSession()\nsession.startTransaction()\ntry {\n  session.getDatabase("college").students.updateOne(\n    { name: "Asha" }, { $inc: { marks: 1 } }\n  )\n  session.commitTransaction()\n} catch (error) {\n  session.abortTransaction()\n} finally { session.endSession() }'}</Code><Table rows={[[<code>mongodump / mongorestore</code>,'Binary backup and restore'],[<code>mongoexport / mongoimport</code>,'JSON or CSV export/import'],[<code>$jsonSchema</code>,'Collection-level schema validation']]}/></section>

      <section id="quick"><h2>Exam Quick Revision</h2><Table rows={[['Create/switch DB',<code>use college</code>],['Insert',<><code>insertOne(doc)</code> / <code>insertMany([docs])</code></>],['Read',<code>find(filter, projection)</code>],['Update',<code>updateOne(filter, {'{'} $set: {'{'}...{'}'} {'}'})</code>],['Delete',<code>deleteOne(filter)</code> / <code>deleteMany(filter)</code>],['Sort high → low',<code>find().sort({'{'} marks: -1 {'}'})</code>],['Aggregation',<code>aggregate([{'{'} $match: ... {'}'}, {'{'} $group: ... {'}'}])</code>],['Unique index',<code>createIndex({'{'} email: 1 {'}'}, {'{'} unique: true {'}'})</code>]]}/><div className="note"><b>Highest value reminders:</b> filter early with <code>$match</code>, use <code>$group</code> for totals, <code>$project</code> to shape output, <code>$lookup</code> for references, and indexes for frequent filters/sorts.</div></section>
      <p style={{textAlign:'center',color:'var(--muted)',fontSize:13}}>End of notes — MongoDB Complete Reference · students dataset</p>
    </div>
  </div>;
}

export default MongoDBNotes;
